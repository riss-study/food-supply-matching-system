=== Phase 2 Task 03 evidence (2026-04-19) — 재평가 기반 축소판 ===

## 전제 재평가

원래 subplan 의 SubTask 3.1~3.4 는 이미 Phase 1~2 작업 과정에서 대부분 구현되어 있었음:

- `audit_log` 테이블 (`backend/docker/mariadb/init/01-schema.sql:163`)
- `AuditLogEntity` + `AuditLogRepository` (`command-domain-supplier`)
- `AdminReviewApplicationService`:
  - approve/hold/reject transition 시 audit_log 에 write (actionType = review_approve/hold/reject, payloadSnapshot 에 reviewId, supplierProfileId, decision, noteInternal, notePublic, reasonCode)
  - detail() 응답에 `reviewHistory[]` (AdminReviewHistoryItemResponse) 포함
- admin-site `ReviewDetailPage.tsx` 에 HistoryItem 컴포넌트 + timeline 렌더링 (actionType / actor / createdAt / noteInternal / notePublic / reasonCode)
- reviews.json 로케일에 actionType.* / detail.actor 등 키 존재

## 이번 세션 조치

1. 현황 감사 — audit write/read/render 전 경로 검증 완료
2. End-to-end smoke test 실행:
   - Admin 로그인 → 검수 queue 조회 → hold 액션 → detail 재조회 → history 1건 확인
   - actionType "review_hold", actor "usr_seed_admin01", notePublic "검증 자료 보완 요청" 정상 렌더
3. 축소 결정 기록:
   - SubTask 3.5 (audit 검색 API) 는 OP-4 로 분리 (실 운영 요구가 생기면 착수)
   - SubTask 3.2 에서 언급된 notice_publish / request_status_change 은 각각 notice / request 테이블에 status + updatedAt/updatedBy 로 이미 추적됨. audit_log 중복 기록은 보류. 필요 시 BE-future.

## End-to-end smoke 로그

Admin login (admin@test.com / Test1234!):
  accessToken OK

First detail (vsub_seed_04, state=submitted):
  history count: 0  ← 아직 결정 전

Hold action:
  POST /api/admin/reviews/vsub_seed_04/hold  → state: hold

Second detail (vsub_seed_04, state=hold):
  history count: 1
  - review_hold by usr_seed_admin01 at 2026-04-19T23:37:24Z note: 검증 자료 보완 요청

## 결론

Task 03 본래 목표 — "관리자가 검수 상세에서 과거 결정 내역을 확인할 수 있다" — 이미 충족. 별도 code change 없이 재평가·검증·문서화로 종결.

Follow-ups (open-items.md):
- OP-4: admin audit-logs 검색 API (실 운영 요구 시)
- BE-future: notice/request 쪽 audit_log 통합 기록 (필요성 재확인 후)
